{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug tests",
      "type": "node",
      "request": "launch",
      "program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
      "args": ["-r", "ts-node/register", "problems/**/*.spec.ts"],
      "cwd": "${workspaceRoot}",
      "protocol": "inspector"
  }
  ]
}
